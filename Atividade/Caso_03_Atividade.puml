@startuml
start
:Acessar tela de login;
:Exibir formulário de credenciais;
:Informar email e senha;
:Obter credenciais;
:Buscar usuário por email;
if (Usuário encontrado?) then (sim)
  :Obter senha do usuário;
  :Validar credenciais;
  if (Credenciais válidas?) then (sim)
    :Criar sessão do usuário;
    :Identificar perfil do usuário;
    :Redirecionar para tela inicial;
  else (não)
    :Exibir mensagem de erro;
  endif
else (não)
  :Exibir mensagem de erro;
endif
stop
@enduml
