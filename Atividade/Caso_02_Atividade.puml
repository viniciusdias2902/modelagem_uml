@startuml
start
:Acessar tela de auto-cadastro;
:Exibir formulário;
:Preencher dados pessoais;
:Obter dados preenchidos;
:Validar dados;
if (Dados válidos?) then (sim)
  :Buscar email no repositório;
  if (Email já cadastrado?) then (sim)
    :Exibir mensagem de erro;
    stop
  else (não)
    :Criar nova instância de Aluno;
    :Salvar aluno no repositório;
    :Exibir mensagem de sucesso;
  endif
else (não)
  :Exibir mensagem de erro;
endif
stop
@enduml
